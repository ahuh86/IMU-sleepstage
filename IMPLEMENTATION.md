# 实施记录

依据：用户确认的“CNN + 单向 RNN 睡眠分期实施方案”（2026-09-09）。

## 工作分解与接口

1. 数据：`imu_rnn.data` 校验/哈希索引，受试者划分，训练归一化，因果序列及磁盘窗口缓存。
2. 模型/指标：`CNNRNN(x, lengths) -> [B,4]`；固定四类指标。
3. 实验：训练、验证早停、恢复最佳权重后测试；单折/LOSO/冒烟/独立推理入口。
4. 验证：单元和合成完整流程、真实 GPU 冒烟、代码审查、完整 13 折训练。

数据记录携带 subject/id/path/start/end/label/segment/sha256 和原始通道统计。Dataset 输出 `(x, label, meta)`；collate 输出 `(x, lengths, labels, metadata)`。磁盘缓存仅存修正长度后的原始六轴，归一化始终按折应用。评估返回逐目标记录和可从记录重算的指标。

## 实现选择

- 在用户指定的空仓库原目录工作，使用 `codex/cnn-rnn` 分支，保留父子仓库结构。
- 当前内存余量有限，采用内容哈希标识的磁盘映射窗口缓存，不全量预加载。
- eval 模式下复用同次评估内的单窗 CNN 特征；已测试与逐序列计算等价。训练不缓存特征。
- 恢复模式仅复用已完成折；未完成折重启，避免伪装成精确的中途恢复。
- 现有解释器为 Python 3.9，使用延迟求值注解保持兼容。补装 pytest 8.4.2 用于验证。

## 当前验证记录

- 数据模块 13 项测试通过。
- 集成后 26 项测试通过，包括完整合成三折、恢复、独立推理、填充隔离和指标重算。
- 最终自动测试 26 项通过（包含实际调用 predict.py 并核对 CSV）。
- 真实 GPU 冒烟完成：11 名训练者各 8 个目标、验证/测试各 32 个目标；训练 1 轮，成功恢复最佳权重并保存预测。结果仅用于流程检查。
- 独立代码审查未发现阻断问题。审查提及原始长度统计与修正长度输入的微小差异；保留原始采样统计口径并在 README 明确记录。
- 完整 13 折 GPU 实验已按用户要求停止，停在第一折 LIHongmei 的第 5 轮；前 4 轮历史与最佳权重保留。尚未完成正式测试折。进程信息在 results/logs/loso-process.json，状态记录为 stopped_by_user；需要继续时按 README 的 --resume 命令重跑未完成折。
- 2026-09-10 参照同工作区 DeepSleepNet 增加 tqdm 逐 batch/验证/测试实时进度、每轮训练与验证汇总、逐类指标表和混淆矩阵；补充 Balanced Accuracy 与 Specificity。
- 2026-09-11 增加性能优先的训练折逆频率类别加权交叉熵。四类权重为 `N/(4*n_c)`，仅由完整训练受试者标签计算；验证/测试隔离，保持自然窗口采样和验证 Macro-F1 选模。类别分布与权重写入逐折 JSON、检查点和结果。
- 完整逆频率加权的 13 折合并 Macro-F1 为 0.20545，低于无权重的 0.21635；Deep/REM 改善不足以抵消 Wake/Light 退化。增加互斥的 `--use-sqrt-class-weight`，以指数 0.5 缩小类别间权重比，作为下一轮性能配置。
